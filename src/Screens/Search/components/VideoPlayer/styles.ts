import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
      video: {
        alignSelf: 'center',
        width: 450,
        height: 400,
        borderRadius: 20,
        marginTop: -15
      },
      buttons: {
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
      },
});